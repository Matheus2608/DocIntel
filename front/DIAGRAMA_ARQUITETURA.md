# 📐 Diagrama da Arquitetura

## Estrutura de Componentes

```
┌─────────────────────────────────────────────────────────────────┐
│                          App.jsx                                 │
│                     (Aplicação Principal)                        │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                      pages/home/Main.jsx                         │
│                      (Orquestrador)                              │
│                                                                  │
│  Estado:                                                         │
│  • currentChat: ChatData | null                                 │
│                                                                  │
│  Hook:                                                           │
│  • useWebSocketChat(chatId, wsUrl)                              │
│    → { messages, isConnected, isTyping, sendMessage }          │
│                                                                  │
│  Lógica:                                                         │
│  • handleUploadSuccess(chatData)                                │
│  • Renderização condicional (Upload vs Chat)                    │
└───┬──────────────────────┬──────────────────────┬───────────────┘
    │                      │                      │
    ↓                      ↓                      ↓
┌─────────┐        ┌──────────────┐      ┌─────────────┐
│ Header  │        │ Upload/Chat  │      │InputMessage │
│         │        │(Condicional) │      │             │
└─────────┘        └──────┬───────┘      └──────┬──────┘
                          │                     │
              ┌───────────┴───────────┐         │
              ↓                       ↓         │
      ┌───────────────┐      ┌───────────────┐ │
      │  Upload.jsx   │      │ChatMessage.jsx│ │
      │               │      │               │ │
      │  Apresenta:   │      │  Apresenta:   │ │
      │  • Dropzone   │      │  • Mensagens  │ │
      │  • Loading    │      │  • Typing     │ │
      │  • Errors     │      │  • Auto-scroll│ │
      │               │      │               │ │
      │  Callback:    │      │  Props:       │ │
      │  onUpload     │      │  • messages   │ │
      │  Success      │      │  • isTyping   │ │
      └───────────────┘      └───────────────┘ │
                                                │
                                                ↓
                                    ┌──────────────────────┐
                                    │  InputMessage.jsx    │
                                    │                      │
                                    │  Estado Local:       │
                                    │  • input: string     │
                                    │                      │
                                    │  Props:              │
                                    │  • isDarkMode        │
                                    │  • isConnected       │
                                    │  • onSendMessage     │
                                    │                      │
                                    │  Lógica:             │
                                    │  • handleSend()      │
                                    │  • handleKeyDown()   │
                                    └──────────────────────┘
```

## Fluxo de Dados

```
┌──────────────────────────────────────────────────────────────────┐
│                    FLUXO DE UPLOAD                                │
└──────────────────────────────────────────────────────────────────┘

Usuário seleciona arquivo
        ↓
┌───────────────────┐
│   Upload.jsx      │
│   handleFile()    │
└─────────┬─────────┘
          │ POST /api/chats
          ↓
┌───────────────────┐
│   Backend API     │
│   createChat()    │
└─────────┬─────────┘
          │ ChatResponse
          ↓
┌───────────────────┐
│ onUploadSuccess() │
│ (callback)        │
└─────────┬─────────┘
          ↓
┌───────────────────┐
│   Main.jsx        │
│ setCurrentChat()  │
└─────────┬─────────┘
          │
          ↓
┌────────────────────────┐
│  useWebSocketChat      │
│  detecta chatId        │
│  conecta ao WebSocket  │
└────────────────────────┘


┌──────────────────────────────────────────────────────────────────┐
│                 FLUXO DE ENVIO DE MENSAGEM                        │
└──────────────────────────────────────────────────────────────────┘

Usuário digita e pressiona Enter
        ↓
┌───────────────────┐
│ InputMessage.jsx  │
│  handleSend()     │
└─────────┬─────────┘
          │ onSendMessage(text)
          ↓
┌───────────────────┐
│     Main.jsx      │
│  sendMessage()    │
└─────────┬─────────┘
          │
          ↓
┌────────────────────────┐
│  useWebSocketChat      │
│  sendMessage(text)     │
└─────────┬──────────────┘
          │
          ├─→ setMessages([...msgs, userMsg])
          │
          └─→ wsRef.current.send(text)
                     ↓
          ┌─────────────────┐
          │  Backend WS     │
          │  onTextMessage  │
          └────────┬────────┘
                   │ Multi<String>
                   ↓
          ┌─────────────────┐
          │ useWebSocketChat│
          │   onmessage     │
          └────────┬────────┘
                   │
                   ├─→ Primeiro chunk: cria mensagem assistant
                   └─→ Chunks seguintes: atualiza mensagem
                            ↓
                   ┌─────────────────┐
                   │ setMessages()   │
                   └────────┬────────┘
                            ↓
                   ┌─────────────────┐
                   │  ChatMessage    │
                   │  renderiza      │
                   └─────────────────┘
```

## Dependências entre Módulos

```
┌──────────────────────────────────────────────────────────────────┐
│                    DEPENDÊNCIAS                                   │
└──────────────────────────────────────────────────────────────────┘

Main.jsx
  ├─ depende de → useWebSocketChat (hook)
  ├─ depende de → Upload (componente)
  ├─ depende de → ChatMessage (componente)
  ├─ depende de → InputMessage (componente)
  └─ depende de → Header (componente)

useWebSocketChat.js
  └─ depende de → WebSocket API (nativa)

ChatMessage.jsx
  └─ depende de → lucide-react (ícones)

InputMessage.jsx
  ├─ depende de → lucide-react (ícones)
  └─ depende de → darkModeBackground (util)

Upload.jsx
  ├─ depende de → lucide-react (ícones)
  └─ depende de → fetch API (nativa)
```

## Estado e Props Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                    ESTADO E PROPS                                 │
└──────────────────────────────────────────────────────────────────┘

Main.jsx (Container)
├─ Estado Local:
│  └─ currentChat: ChatData | null
│
├─ Hook useWebSocketChat retorna:
│  ├─ messages: Message[]
│  ├─ isConnected: boolean
│  ├─ isTyping: boolean
│  ├─ sendMessage: (text: string) => boolean
│  └─ clearMessages: () => void
│
└─ Passa para filhos:
   │
   ├─ Header
   │  ├─ headerText: string
   │  └─ paragraphText: string
   │
   ├─ Upload
   │  └─ onUploadSuccess: (ChatData) => void
   │
   ├─ ChatMessage
   │  ├─ messages: Message[]
   │  └─ isTyping: boolean
   │
   └─ InputMessage
      ├─ isDarkMode: boolean
      ├─ isConnected: boolean
      └─ onSendMessage: (text: string) => boolean

┌──────────────────────────────────────────────────────────────────┐
│                    TIPOS DE DADOS                                 │
└──────────────────────────────────────────────────────────────────┘

type ChatData = {
  id: string;
  createdAt: string;
  updatedAt: string;
  hasDocument: boolean;
}

type Message = {
  role: 'user' | 'assistant';
  text: string;
}

type WebSocketState = {
  messages: Message[];
  isConnected: boolean;
  isTyping: boolean;
  sendMessage: (text: string) => boolean;
  clearMessages: () => void;
}
```

## Ciclo de Vida

```
┌──────────────────────────────────────────────────────────────────┐
│                    CICLO DE VIDA                                  │
└──────────────────────────────────────────────────────────────────┘

1. MONTAGEM INICIAL
   ↓
   Main.jsx renderiza
   ├─ currentChat = null
   ├─ useWebSocketChat(null, wsUrl)
   │  └─ Não conecta (sem chatId)
   └─ Renderiza <Upload />

2. APÓS UPLOAD
   ↓
   onUploadSuccess(chatData)
   ├─ setCurrentChat(chatData)
   ├─ useWebSocketChat detecta chatId
   │  ├─ Cria WebSocket
   │  ├─ ws.onopen → setIsConnected(true)
   │  ├─ ws.onmessage → recebe "Welcome..."
   │  └─ setMessages([welcomeMsg])
   └─ Renderiza <ChatMessage />

3. ENVIO DE MENSAGEM
   ↓
   InputMessage.handleSend()
   ├─ onSendMessage(text)
   ├─ sendMessage(text)
   │  ├─ setMessages([...msgs, userMsg])
   │  ├─ setIsTyping(true)
   │  └─ ws.send(text)
   └─ setInput('')

4. RECEBIMENTO DE RESPOSTA
   ↓
   ws.onmessage
   ├─ Primeiro chunk
   │  ├─ currentStreamRef = { role: 'assistant', text }
   │  ├─ setMessages([...msgs, streamRef])
   │  └─ setIsTyping(false)
   └─ Chunks seguintes
      ├─ currentStreamRef.text += chunk
      └─ setMessages([...msgs.slice(0,-1), streamRef])

5. DESMONTAGEM
   ↓
   useEffect cleanup
   ├─ shouldStop = true
   ├─ ws.close()
   └─ currentStreamRef.current = null
```

## Arquitetura de Camadas

```
┌──────────────────────────────────────────────────────────────────┐
│                     APRESENTAÇÃO                                  │
│  (Componentes UI puros - sem lógica de negócio)                  │
│                                                                   │
│  • ChatMessage.jsx  → renderiza mensagens                        │
│  • Header.jsx       → renderiza cabeçalho                        │
└──────────────────────────────────────────────────────────────────┘
                              ↑
┌──────────────────────────────────────────────────────────────────┐
│                     CONTROLE                                      │
│  (Componentes com estado local)                                   │
│                                                                   │
│  • InputMessage.jsx → gerencia input                             │
│  • Upload.jsx       → gerencia upload e validação                │
└──────────────────────────────────────────────────────────────────┘
                              ↑
┌──────────────────────────────────────────────────────────────────┐
│                     ORQUESTRAÇÃO                                  │
│  (Coordena estado global e comunicação)                           │
│                                                                   │
│  • Main.jsx → orquestra tudo                                     │
└──────────────────────────────────────────────────────────────────┘
                              ↑
┌──────────────────────────────────────────────────────────────────┐
│                     LÓGICA DE NEGÓCIO                             │
│  (Hooks customizados - lógica reutilizável)                       │
│                                                                   │
│  • useWebSocketChat.js → gerencia WebSocket                      │
└──────────────────────────────────────────────────────────────────┘
                              ↑
┌──────────────────────────────────────────────────────────────────┐
│                     SERVIÇOS EXTERNOS                             │
│  (APIs e integrações)                                             │
│                                                                   │
│  • WebSocket API    → ws://backend/document-support-agent        │
│  • REST API         → http://backend/api/chats                   │
└──────────────────────────────────────────────────────────────────┘
```
